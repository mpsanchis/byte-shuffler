# Byte Shuffler

## Description

Byte Shuffler is a tool designed to shuffle the bytes of a file. It can be used to create a new file with the bytes of the original file in a different order.

## TO DO

- take several files as input, or from config file (batch enc/dec)
- potentially, support encryption/decryption with key